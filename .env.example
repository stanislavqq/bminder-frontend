# Frontend bind address and port (Vite dev / Docker publish)
# HOST must be an IP for Docker: 127.0.0.1 or 0.0.0.0
HOST=127.0.0.1
PORT=5173

# Backend API origin, without trailing slash
API_HOST=http://127.0.0.1:3333
